package com.motorbike.anqi.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.motorbike.anqi.bean.UsreBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "USRE_BEAN".
*/
public class UsreBeanDao extends AbstractDao<UsreBean, String> {

    public static final String TABLENAME = "USRE_BEAN";

    /**
     * Properties of entity UsreBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property UserName = new Property(0, String.class, "userName", false, "USER_NAME");
        public final static Property UserId = new Property(1, String.class, "userId", true, "USER_ID");
        public final static Property HeanImg = new Property(2, String.class, "heanImg", false, "HEAN_IMG");
    }


    public UsreBeanDao(DaoConfig config) {
        super(config);
    }
    
    public UsreBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"USRE_BEAN\" (" + //
                "\"USER_NAME\" TEXT," + // 0: userName
                "\"USER_ID\" TEXT PRIMARY KEY NOT NULL ," + // 1: userId
                "\"HEAN_IMG\" TEXT);"); // 2: heanImg
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"USRE_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, UsreBean entity) {
        stmt.clearBindings();
 
        String userName = entity.getUserName();
        if (userName != null) {
            stmt.bindString(1, userName);
        }
 
        String userId = entity.getUserId();
        if (userId != null) {
            stmt.bindString(2, userId);
        }
 
        String heanImg = entity.getHeanImg();
        if (heanImg != null) {
            stmt.bindString(3, heanImg);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, UsreBean entity) {
        stmt.clearBindings();
 
        String userName = entity.getUserName();
        if (userName != null) {
            stmt.bindString(1, userName);
        }
 
        String userId = entity.getUserId();
        if (userId != null) {
            stmt.bindString(2, userId);
        }
 
        String heanImg = entity.getHeanImg();
        if (heanImg != null) {
            stmt.bindString(3, heanImg);
        }
    }

    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1);
    }    

    @Override
    public UsreBean readEntity(Cursor cursor, int offset) {
        UsreBean entity = new UsreBean( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // userName
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // userId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2) // heanImg
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, UsreBean entity, int offset) {
        entity.setUserName(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setUserId(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setHeanImg(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
     }
    
    @Override
    protected final String updateKeyAfterInsert(UsreBean entity, long rowId) {
        return entity.getUserId();
    }
    
    @Override
    public String getKey(UsreBean entity) {
        if(entity != null) {
            return entity.getUserId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(UsreBean entity) {
        return entity.getUserId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
